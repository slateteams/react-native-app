# Resolve react_native_pods.rb with node to allow for hoisting
require Pod::Executable.execute_command('node', ['-p',
  'require.resolve(
    "react-native/scripts/react_native_pods.rb",
    {paths: [process.argv[1]]},
  )', __dir__]).strip

# Standard CocoaPods sources only for clean React Native build
source 'https://cdn.cocoapods.org/'

platform :ios, '16.0'
prepare_react_native_project!

use_frameworks! :linkage => :static

target 'slateReactNative' do
  config = use_native_modules!

  use_react_native!(
    :path => config[:reactNativePath],
    :app_path => "#{Pod::Config.instance.installation_root}/..",
    :hermes_enabled => true,
    :fabric_enabled => true
  )

  # 🎬 SLATE INTEGRATION - Temporarily disabled for stable bridge development
  # pod 'Slate/ReactNative', :path => '../../ios-app'

  post_install do |installer|
    # Standard React Native post-install
    react_native_post_install(
      installer,
      config[:reactNativePath],
      :mac_catalyst_enabled => false
    )
    
    # 🎬 CLEAN REACT NATIVE BUILD - No additional configuration needed
  end
end